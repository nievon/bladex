<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Russo+One&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/public/css/style.css">
</head>
<body>
<header>
    <div class="container-header">
        <a class="header-link" href="http://bladexphp.ru/">Home</a>
        <a class="header-link-logo" href="/">
            <img style="opacity: 0.75; width: 152px; height: 52px;" src="image1.png" alt="logo">
        </a>
        <a class="header-link" href="https://github.com/nievon/bladex">GitHub</a>
    </div>
</header>
<main>
    <div class="container">
        <h1>{{ title }}</h1>
        <p>{{ text }}</p>
        {% if posts is not defined or posts|length == 0 %}
            <a href="posts">Тест ORM</a>
        {% else %}
            <a href="/">Главная</a>
        {% endif %}
    </div>
    <div class="d-flex">
        {% if posts is defined and posts|length > 0 %}
            {% for post in posts %}
                <div class="container">
                    <h2>{{ post.title }}</h2>
                    <p>{{ post.text }}</p>
                </div>
            {% endfor %}
        {% endif %}
    </div>
</main>
</body>
</html>
